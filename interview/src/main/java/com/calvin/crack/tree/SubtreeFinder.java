package com.calvin.crack.tree;

public class SubtreeFinder {
	public static class Node {
		private Node left;
		private Node right;
		private int data;		
	}
	
	/**
	 * Find root2 in root1
	 * Traverse root2 to build a queue of visited node
	 * Traverse root1, dequeue front of queue, compare node
	 * @param tree
	 * @param tree2
	 * @return
	 */
	public boolean isSubtree(Node root1, Node root2){
		
		return false;
	}
}
