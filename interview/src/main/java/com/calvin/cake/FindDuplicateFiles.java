package com.calvin.cake;

public class FindDuplicateFiles {
	public void find(){
		// Loop thru all files in the operation system
		// List out all drives
		// For each drive
		// 	List out all files
		//		For each file
		//			map.put(hash(file), list<String> names)
		// How to tell which one is the duplicate?
		// a) base on names
		// b) base on timestamp
		
		// We can skip all files and folders that haven't been updated
	}
}
